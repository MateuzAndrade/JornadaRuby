<%= form_with(model: manager) do |form| %>
  <% if manager.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(manager.errors.count, "error") %> prohibited this manager from being saved:</h2>

      <ul>
        <% manager.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :sector_id, style:"display:block"%>
    <%= form.collection_check_boxes :sector_ids, Sector.all, :id, :description do |s| %>
      <%= s.check_box %>
      <%= s.label %>
    <% end %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
